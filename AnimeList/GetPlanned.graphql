query GetPlanned($userId: Int!) {
    MediaListCollection(userId: $userId , status_in: [PLANNING], type: ANIME) {
        lists {
            isCustomList
            entries {
                info: media {
                    title {
                        userPreferred
                    }
                }
                progress
            }
        }
    }
}
